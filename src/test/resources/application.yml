#spring:
#  datasource:
#    url: jdbc:h2:mem:test   #메모리 모드로 테스트
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create      # 매 애플리케이션 실행 시 db 초기화
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true
# 위의 설정 다 없어도 스프링부트 default 설정으로 알아서 메모리모드로 실행함

logging:
  level:
    org.hibernate.sql: debug    #show_sql 은 sout으로 출력하므로 사용 지양. 운영 측면에서 로그로 보관해야 함